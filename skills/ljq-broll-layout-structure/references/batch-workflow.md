# 多片段工作流

每个连续镜头建立独立案例目录，共享同一主渲染器，不复制环境。按剪辑手法或镜头段分组，而不只按文件名分组。

对每个案例依次执行：确认预检；选择一个落定帧；拆分稳定元素；写 layout 与 Remotion 静态构图；渲染 still；校准；运行合同校验。单个案例失败时记录阶段和错误，不把它算作成功。

批次汇总必须列出案例 ID、源片段、落定帧、组件、可替换内容、状态和待复核项。验收 still 是证据，不是下一次复用的结构模板。
